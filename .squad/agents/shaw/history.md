# Shaw — Project History

## Core Context

- **Owner:** Vincent Verweij
- **Project:** Tracey — Window Activity Timetracking Tool
- **Branch:** `001-window-activity-tracker`
- **Stack:** Playwright E2E (against full Tauri app) + xUnit (.NET 10) + cargo test (Rust)
- **My files:** `tests/e2e/` (Playwright), `src/Tracey.Tests/` (xUnit)
- **Spec:** `specs/001-window-activity-tracker/spec.md` — 9 user stories, all acceptance scenarios map to my tests
- **TDD rule:** Failing tests MUST be committed before implementation begins. No exceptions.
- **Created:** 2026-03-15

## Learnings

### 2026-03-15: Team Setup & Test Strategy
- 9 user stories: US1 (timer), US2 (idle), US3 (client/project/task mgmt), US4 (screenshot timeline), US5 (fuzzy match), US6 (tags), US7 (reports), US8 (settings), US9 (multi-device sync)
- GDI screenshot capture stubbed via `#[cfg(feature="test")]` — Playwright runs use `--features test` build (Fusco configures CI)
- Playwright fixture provides IPC overrides for idle detection and screenshot triggers (no real OS events needed in E2E)
- Orphaned autocomplete has its own E2E test (T025a): create entry → delete project → type description → verify orphan indicator
- Overlap warning modal is tested: create overlapping manual entry, verify warning shown, verify `force: true` saves
- Reviewer gate: I hold test coverage gate. Shaw rejects → different agent revises (never original author)
- Rust coverage target: ≥ 80% branch coverage for business logic
- Tasks needing my initial tests (first): T018, T019 (US1), T026, T027 (US2), then US3–US9 in order

### 2026-03-15: T003 — xUnit test project initialized
- .NET version used: **net10.0** (SDK 10.0.104 confirmed installed)
- Solution file format: `Tracey.slnx` (new XML-based format — `dotnet sln add` works with it)
- `dotnet build Tracey.slnx` — **passed**, 0 errors, 0 warnings
- `dotnet test Tracey.Tests/Tracey.Tests.csproj` — **EXIT:0**, "No test is available" (correct: empty placeholder classes, no [Fact] methods)
- Placeholder stubs created: `TimerStateServiceTests.cs` (T019/US1) and `FuzzyMatchTests.cs` (US5)
- UnitTest1.cs deleted (was auto-generated by `dotnet new xunit`)
- Project reference Tracey.Tests → Tracey.App added via `dotnet add reference`
- NOTE: Tracey.App uses `Microsoft.NET.Sdk.BlazorWebAssembly` SDK — references are fine for test compilation; WASM-specific targets won't run in xUnit context (Blazor logic under test must be kept in plain C# classes, not in .razor components)
- Terminal was shared with Finch's T002 (MailKit/Blazor WASM setup) — output got mixed; used `--no-build` and subprocess redirection to isolate

### 2026-03-15: T004 — Playwright E2E test project initialized
- Playwright version: **1.58.2**
- Chromium installed: Chrome Headless Shell 145.0.7632.6 (chromium-headless-shell v1208)
- Location: `tests/e2e/` — npm init, @playwright/test installed as dev dependency
- `playwright.config.ts` created: `workers: 1`, `fullyParallel: false` (single Tauri app instance constraint), chromium project only
- Spec stubs created (no tests yet — T018 writes real tests): `timer.spec.ts`, `idle.spec.ts`, `projects.spec.ts`, `timeline.spec.ts`, `quickentry.spec.ts`
- `.gitignore` covers `node_modules/`, `dist/`, `test-results/`, `playwright-report/`

### 2026-03-15: US1 Acceptance Scenarios for T018 (Playwright — timer.spec.ts)
All 5 scenarios from spec.md US1 must become failing tests before Reese starts T020:
1. **Fuzzy dropdown**: type `project/task/description` → live dropdown appears sorted by match strength
2. **Tab/Enter confirm**: live dropdown visible → Tab/Enter confirms segment, cursor moves to next segment
3. **Auto-stop on new start**: complete entry + Enter → new timer starts, previously running timer auto-stopped and saved
4. **Stop saves UTC/local**: stop running timer → entry saved with correct UTC start/end, displayed in user's local timezone
5. **Continue copies entry**: click Continue on past entry → new timer starts from current time, description/project/task copied
Additional tests beyond the 5 spec scenarios (from tasks.md):
- T018 also covers: **overlap-warning modal** (manual entry overlapping existing → warning shown; `force: true` override → saves)
- T025a: orphaned autocomplete indicator (delete project → autocomplete shows orphan warning on suggestion)
- T029a: scroll-position preserved after navigation away and back
- T030c: inline edit auto-save on blur (no manual save button needed)
