# Shaw — T031: US2 Idle Detection E2E Tests
**Date:** 2026-03-16  
**Agent:** Shaw (QA/Test)  
**Task:** T031

## Outcome

7 failing E2E tests committed to `tests/e2e/specs/idle-detection.spec.ts`. TypeScript: 0 errors. TDD gate open — tests will fail with `TimeoutError` once app runs but before UI is implemented.

## Tests Written

1. `idle modal does NOT appear when no timer is running`
2. `idle modal appears after inactivity threshold when timer is running` — also asserts all 4 buttons present
3. `"Keep" option closes modal without creating new entry` — timer stays running
4. `"Break" option stops timer at idle start and creates break entry` — verifies Timeline shows both entries
5. `"Meeting" option creates a meeting entry for the idle period` — verifies Timeline shows meeting text
6. `"Specify" option shows description input and creates entry` — inline input within modal, entry in Timeline
7. `idle threshold uses value from preferences (set via IPC)` — 300s threshold → modal does NOT appear within 3s

## Contracts Root Must Honour

- Idle modal: `role="dialog"` with accessible name matching `/idle|away|back/i`
- Option buttons: `role="button"` names `/break/i`, `/meeting/i`, `/specify/i`, `/keep/i`
- Timer display: `role="timer"` visible while a timer is running
- Specify flow: `role="textbox" name=/description|what were you doing/i` **within** same modal; `role="button"` matching `/save|confirm/i`
- Timeline nav link: `role="link"` with name `/timeline/i`
- Quick-entry bar: `role="textbox"` matching `/what are you working on/i`

## IPC Notes

- `preferences_update` with `{ update: { inactivity_timeout_seconds: 5 } }` sets fast threshold in every test
- `timer_stop` called with try/catch at test start — `"no_active_timer"` error is safe to ignore
- `window.__TAURI_INTERNALS__.invoke(...)` in `page.evaluate()` with `@ts-ignore` — Tauri JS types unavailable in test env

## Decisions Logged

→ decisions.md: Phase 4 / Shaw T031
